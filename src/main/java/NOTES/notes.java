package NOTES;

public class notes {
    // Write all basic things in controller. inside controller we call the methods that is defined in
    // services. Inside servies we call predefined functions which is written inside repository.
    // inside repository we extends Mongo repository which has all services inbuilt
    /*
    Controller is main area where everything runs.
    Controller calls services . services calls repository


    Application.yml : it is used instead of application properties . its another method of editing the properties of
    our app. We can use either application.properties OR application.yml OR command line to edit things like
    server port , host , context path.
    (Refer yt video in playlist of Engineering Digest : Video no. 23 -> Properties of YAML

    DOUBTS ------>  Mockito Spring boot tutorial video no. 25
     */
}
